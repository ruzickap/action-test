git:
  tagName: v${version}
  commitMessage: Release v${version}
  changelog: npx auto-changelog --stdout --commit-limit false --unreleased --template ../changelog.hbs
  requireCommits: true
github:
  release: true
  releaseName: Broken link checker v${version}
hooks:
  before:release: auto-changelog --commit-limit false --template ../keepachangelog && git add CHANGELOG.md
